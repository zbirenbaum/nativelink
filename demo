term 1:
ulimit -n 65000 && cargo run --bin nativelink -- scheduler_worker.json

term 2:
ulimit -n 65000 && cargo run --bin nativelink -- only_scheduler.json

term 3:
cargo run --bin nativelink -- cas.json

term 4:
bazel test //... --test_tag_filters=demo --build_tag_filters=demo \
--remote_executor=grpc://127.0.0.1:61051 \
--remote_cache=grpc://127.0.0.1:50051 \
--remote_instance_name=main
